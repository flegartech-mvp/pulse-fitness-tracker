[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: BrowserRouter handles routing client-side, so every
# unmatched path must serve index.html (200, not a redirect).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
