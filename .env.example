# Pulse — Fitness Tracker
# This app is fully local-first: no backend, no account, no API keys required.
# Environment variables are only needed if you add a remote backend in the future.

# Example: if you add analytics
# VITE_ANALYTICS_ID=

# Example: if you add a backend API
# VITE_API_URL=http://localhost:8000
